function soma(x = 1, y = 2) {
  const resultado = x + y;
  return resultado;
}

const resultado = soma(2, 5);
console.log(resultado);

const raiz = (n) => {
  return n ** 0.5; // é o mesmo que elevar ao quadrado
};

console.log(raiz(9));
console.log(raiz(30));
